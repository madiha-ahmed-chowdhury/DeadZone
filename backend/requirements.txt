# DeadZone backend runtime dependencies
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4
python-dotenv==1.0.1

# Supabase + Postgres
supabase==2.7.1
httpx==0.27.0

# H3 geospatial indexing
h3==3.7.7

# Telegram bot
python-telegram-bot[ext]==21.3

# WebSockets for the realtime bridge
websockets==12.0

# CORS / HTTP helpers (transitive but pinned for clarity)
anyio==4.4.0